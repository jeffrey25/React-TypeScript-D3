// import { shallow } from "enzyme";
// import * as React from "react";
// import * as d3force from "d3-force";
// import { expect } from "chai";
// import Nodes from "../src/components/nodes";
// //import data from "../src/miserables";
// import mbaData from "../src/mba_med";

// describe("Nodes", () => {
//   it("renders the nodes", () => {
//     const simulation = d3force.forceSimulation()
//       .nodes(mbaData.nodes);
//     const wrapper = shallow(<Nodes nodes={mbaData.nodes} simulation={simulation} />);
//     expect(wrapper.children().length).to.equal(mbaData.nodes.length);
//   });
// });
